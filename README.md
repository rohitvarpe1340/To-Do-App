# To-Do App

A full-stack To-Do application built with **Angular** (frontend) and **Node.js + Express + MySQL** (backend).

---

## 📝 Project Overview

This application allows users to:

- Create, view, edit, and delete tasks.
- Organize tasks by categories or priorities (optional enhancement).
- Manage tasks with a simple and user-friendly UI.

The project is structured into frontend and backend folders for clear separation of concerns.

---
## ⚙️ Frontend

- Built using **Angular 17+**.
- Handles UI, routing, and API calls to the backend.
- Important files/folders:
  - `src/app` → main application logic
  - `src/index.html` → main HTML page
  - `angular.json` → Angular project configuration
  - `package.json` → frontend dependencies

## Frontend 
# - cd frontend
# - npm install
Run - ng serve

##Backend
# - cd backend
# npm i
Run - npm run dev
